# Tech Lead Takeover

Bắt đầu read-only.

1. Đọc core authority files.
2. Chạy `python scripts/ai_os.py check` và không bypass lỗi.
3. Kiểm tra Git branch, HEAD, status, recent commits.
4. Kiểm tra process, PID/port/log/cleanup.
5. Kiểm tra evidence/output gần nhất.
6. Đối chiếu fingerprint và lease/writer sống.
7. Trả Startup Brief mở rộng, mismatch và next exact action.

Không dùng giả định từ chat cũ, không destructive Git và không chỉnh checkpoint để che mismatch.
