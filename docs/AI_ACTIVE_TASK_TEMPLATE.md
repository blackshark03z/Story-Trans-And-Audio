# Legacy / superseded task template

The pre-v1.16 template is retained verbatim at
`docs/ai-build-os/provenance/pre-v1.16/AI_ACTIVE_TASK_TEMPLATE.md` for
historical recovery only.

Use the v1.16 task templates under `templates/` through the Build OS lifecycle
after the owner explicitly initializes a Product Contract. This file must not
be used to create a competing Active Task record.
