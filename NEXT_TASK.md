# Next Task

Current Status:
Task 18AV accepted repair block `#1` for Chapter `368` Segments `665`/`666` and reassembled the same Job `22` into active artifact `84`. The original artifact `81` is now stale and the candidate audio remains preserved at its original path.

Current Baseline:
- Branch `main`
- Verify current `HEAD` with `git log -1` before starting the next task
- Canonical production runtime: `http://127.0.0.1:8772` -> `D:\Youtube\Story Trans And Audio\data`
- YouTube Auto must remain untouched on `http://127.0.0.1:8765`
- Protected untracked paths must remain untouched:
  - `experiment_b_transcript/`
  - `runs/`
- Chapter `368`: ID `368`, title `Chương 368`, active approved Text Revision `736`, active approved Casting Plan `23` revision `1`, job `22`, job chapter `22`, live repair block `1` `accepted`, active artifact `84`

Next Recommended Task:
Task 18AW - Final Human Audio QA of Reassembled Chapter 368 Artifact.
