# Next Task

Task classification:
AWAITING_OPERATOR_DECISION

Current strategic state:
PRODUCTION_READY / PRODUCTION_OPERATIONS

Current status:
Documentation reconciliation reset the project direction to `PRODUCTION OPERATIONS AND ON-DEMAND HARDENING`. No system-development milestone is automatically active, and Task 18BD-S4 is not automatically authorized.

Current baseline:
- Branch `main`
- Expected current baseline before new work: `9e7a01dee5471710136687572eecbf2e7b6cc602`
- Canonical Story Audio runtime: `http://127.0.0.1:8772`
- Runtime schema: `12`
- Protected untracked paths must remain untouched:
  - `experiment_b_transcript/`
  - `runs/`
- Completed chapters: `364` artifact `69`, `365` artifact `72`, `366` artifact `78`, `367` artifact `75`, and `368` artifact `84` are documented as `HUMAN_QA_PASS`.
- Chapter `369`: active Text Revision `738`, Speaker Assignment Draft `15` approved, Casting Plan `24` revision `1` draft/unapproved, no Jobs, no JobChapters, no production segments, no production attempts, no artifacts, no active audio, and audio status `not_created`.
- Preview provenance guard is complete. Quarantined legacy custom previews must not be used as listening evidence.

Authorized action:
No provider, TTS, preview, Casting Plan, job, artifact, database, or audio mutation is authorized until the operator chooses the next direction.

Operator decision required:
A. Continue Chapter 369 using the already-valid `unknown_fallback=narrator` policy.

B. Resume optional distinct-voice selection for the two unnamed Chapter 369 dialogue rows as an editorial production-quality choice.

C. Defer Chapter 369 and explicitly activate a separate system roadmap item.

Important:
`NEXT_TASK.md` records one executable next action or decision checkpoint. It must conform to `ROADMAP.md` and may not silently redefine project strategy.
